{
  "_help": {
    "usage": "单个模型 + 单个提示词调用测试配置。配置好 .env 后，可以直接运行 python compare_prompts.py。",
    "env_file": "本地环境变量文件路径。填写相对本配置文件的路径或绝对路径；常用 ../.env。",
    "cases_file": "测试用例文件。可填写单个 JSON 对象路径，或 JSON 数组文件路径；数组每项可以是字符串，或包含 id/input 的对象。",
    "output_dir": "运行结果输出目录。填写目录路径；不存在时会自动创建。",
    "timeout_seconds": "单次模型 API 请求超时时间，单位秒。建议 30-180。",
    "max_concurrency": "并发数。单模型单提示词测试建议保持 1，便于观察单次调用结果。",
    "defaults.temperature": "默认采样温度。范围通常 0-2；结构化输出和回归测试建议 0-0.3。",
    "defaults.max_tokens": "默认最大输出 token 数。按任务需要填写；结构化短输出可 1000-3000，长文本可更高。",
    "prompt_files": "本配置只保留一个启用的提示词文件，用于单提示词输入输出测试。",
    "prompt_files[].name": "提示词版本名，会显示在报告中。单提示词测试可使用 current 或 baseline。",
    "prompt_files[].path": "提示词文件路径。填写 .md/.txt 等文本文件路径。",
    "prompt_files[].enabled": "是否启用。单提示词测试保持 true。",
    "providers": "服务商公共配置。key 是自定义服务商名称，会被 models[].provider 引用。",
    "providers.*.type": "服务商类型。当前仅支持 openai_compatible。",
    "providers.*.base_url": "OpenAI-compatible API base URL，不要包含 /chat/completions。",
    "providers.*.api_key_env": "读取 API key 的环境变量名，需要在 env_file 或系统环境变量中设置。",
    "models": "本配置只保留一个启用的模型，用于观察单模型调用结果。",
    "models[].name": "本地展示名称。建议用服务商_模型，例如 zai_glm_5_1。",
    "models[].provider": "引用 providers 里的服务商名称，例如 zai。",
    "models[].model": "服务商实际模型名。按对应服务商文档填写。",
    "models[].temperature": "可选，覆盖 defaults.temperature。范围通常 0-2。",
    "models[].max_tokens": "可选，覆盖 defaults.max_tokens。",
    "models[].enabled": "是否启用。单模型测试保持 true。"
  },
  "env_file": "../.env",
  "cases_file": "../cases/single_input.example.json",
  "output_dir": "../runs",
  "timeout_seconds": 60,
  "max_concurrency": 1,
  "defaults": {
    "temperature": 0.5,
    "max_tokens": 10000
  },
  "prompt_files": [
    {
      "name": "current",
      "path": "../prompts/system.intent.example.md",
      "enabled": true
    }
  ],
  "providers": {
    "zai": {
      "type": "openai_compatible",
      "base_url": "https://api.z.ai/api/paas/v4",
      "api_key_env": "ZAI_API_KEY"
    }
  },
  "models": [
    {
      "name": "zai_glm_4_6",
      "provider": "zai",
      "model": "glm-4.6",
      "enabled": true
    }
  ]
}
